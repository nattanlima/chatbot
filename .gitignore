# Dependências de teste (não vão para o GitHub Pages)
node_modules/

# Segredos locais do Worker (NUNCA versionar)
.dev.vars
worker/.dev.vars

# Estado local do wrangler
.wrangler/
worker/.wrangler/

# Logs
npm-debug.log*
*.log

# Configs locais de agentes de IA (Codex/Cursor) e cache do detector de design
.codex/
.cursor/
.impeccable/hook.cache.json
